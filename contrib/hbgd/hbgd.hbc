description=libgd wrapper (imaging)

incpaths=.

headers=gd.ch

libs=${_HB_DYNPREF}${hb_name}${_HB_DYNSUFF}

# NOTE: Old Windows binary distros used `bgd` as the name.
{!linux}libs=gd
{linux}libs=gd png jpeg freetype m
{darwin}libpaths=/usr/local/opt/gd/lib

libs=hbct.hbc
